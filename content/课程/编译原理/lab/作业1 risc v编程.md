内存布局：
┌─────────────┐ 高地址
│    栈段     │ ← 动态增长（向下）
├─────────────┤
│    堆段     │ ← 动态分配
├─────────────┤
│   数据段    │ ← 存储全局变量
├─────────────┤
│   代码段    │ ← 存储指令（只读）
└─────────────┘ 低地址

`lla`是"load local address"，获取标签的绝对地址