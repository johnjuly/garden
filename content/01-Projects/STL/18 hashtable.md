---
created: 2026-05-06
tags:
  - cpp
  - stl
---
放一大堆元素。元素折射出一个编号。理想情况：2^32种变化，空间足够：sizeof(T)\*2^32。

碰撞 后一个挪到其他地方，根据一次或二次方程式。
separate chaining: 一个链表 串在一起。链表太长，打散，人的经验法则。元素个数比篮子个数还要多，篮子增加两倍，元素落在的篮子重新计算。
![[Pasted image 20260506091323.png]]
质数作为篮子量。选择 倍数附近的质数。53->97

## 实现

![[Pasted image 20260506092001.png]]
- 六个模板参数。得到编号：HashFcn,
- 多大 三个函数对象(1\*3)，vector(三个指针4\*3),登记元素个数(4) 19->20

## 应用

![[Pasted image 20260506093029.png]]
c风格字符串 ，两个指针。看内容相等。eqstr.利用strcmp函数，两个指针所指的相比。接口不一致，该函数返回类型 不是bool. -1.0.1

## hash函数，返回hash-code.

![[Pasted image 20260506093318.png]]
- 偏特化。若是数值，本身当作编号。hash若指明类型的话，成为functor. 字符串转成编号，每个字符走一遍 ascii码![[Pasted image 20260506093700.png]]


--- 


不定序的容器，改名由hash变为unordered_;

只要元素个数等于篮子个数，篮子会扩充，rehashing

第二讲到此结束 容器的探讨。