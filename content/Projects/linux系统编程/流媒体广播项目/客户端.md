---
created: 2026-04-03
---

## 命令行

通过管道传输 命令行获取参数opt


用一个结构体 存储命令行的值.
命令行的分析 getopt_long  

每个case break是因为 每个函数/功能都有失败的可能

---

## socket


实现多播 setsockopt  `man 7 ip`
`int setsockopt(int sockfd,int level,int optname,const void*optval,socklen_t optlen)`

对某一层的属性进行设置 

包含头文件不想使用/`#include "../include/proto.h"`时，在Makefile里指定`CFLAGS+=-I../include` ,则可使用`#include <proto.h>`


## 管道