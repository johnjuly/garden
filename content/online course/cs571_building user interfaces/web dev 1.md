
> react basics

# 定义:

也叫做RecatJS. 是一个JS library for building user interface
- react中加入css 使用 javascript object 双curly braces`{react} <div style ={{border: "doteed",padding:"irem"}}>`
- 使用classname 而不是class的原因： class 是 js的 reserved #待查   word;class  means javascript class
- 使用state use state的原因：是当调用usestate函数时会出发rerender 网页
- set state async
- useeffect 使得fetch data不会陷入循环 too many requests