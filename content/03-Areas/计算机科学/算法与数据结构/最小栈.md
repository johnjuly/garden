---
created: 2026-04-14
tags:
  - 栈
topic: 数据结构
field: cs
type: course-note
---
![[Pasted image 20260414131913.png]]
两个栈 data&min;
data:正常栈，接收数字，弹出数字
min:同步状况 ，同步更新
逻辑：
1. 当 当前数字小于等于min顶，min压当前数字
2. 当 当前数字大于Min，min压入 当前的min顶
3. 当 min空说明data也为空，初始的时候,同步压入当前数字


```cpp

void push(int val){
	data.push(val);
	
	if(min.isEmpty()||val<=min.peek())
	{
		min.push(val);
	}else{
	//重复压入
		min.push(min.peek());
	}

}

//同步弹出
void pop(){
	data.pop();
	min.pop();
}

int top(){
	return data.peek();
}
int getMin(){
	return min.peek();
}
```


## 优化

自己实现栈 ，使用数组

```cpp
data = new int[MAXN];
min=new int[MAXN];
size=0;

void push(int val){
	data[size]=val;
	if(size==0||val<=min[size-1]){
		min[size]=val;
	}else {
		min[size]=min[size-1];
	}
	size++;
}

void pop(){
	size--;
}
int top(){
	return data[size-1];
}

```
