---
created: 2026-05-03
tags:
  - 生活/书籍
  - cs/并发
type: clip
---
- 将计算过程抽象成表达式求值。->表达式由纯数学函数构成，->数学函数是第一类对象且没有副作用
- 锁的规则：针对线程之间共享的可变的数据，即共享可变状态；函数式编程无可变状态，故没有该状态带来的问题。


## 函数式编程基础，并行化一个函数式算法

- wikipedia词频统计。
	1. 接受wikipedia xml dump,返回dump中的页面序列
	2. 接受一个页面，返回页面上的词序列
	3. 接受一个词序列，返回含有词频的map
- 用序列的懒惰特性来处理较大的序列，甚至无穷序列。
- 序列处理函数
## Clojure的reuducer框架下并行化

### 每次一页
```clojure
(defn count-words-parallel [pages]
	( reduce (partial merge-with +)
	(pmap #(frequencies(get-words %)) pages))	
	)
```

- 逐页地计数合并会导致大量合并操作，对页面进行批处理，减少合并操作次数
- 将100个页面作为一个批次进行处理
```clojure
(defn count-words [pages]
	(reduce (partial merge-with +)
	(pmap count-words-sequential (partition-all 100 pages))
	)
)

```


- 化简器： 不代表函数返回的结果，代表如何产生结果的描述。
- fold使用分而治之的策略 将化简操作并行化。
## 并发函数式WEB服务

### 数据流式编程

异步求值，求值前会阻塞线程。
####  future模型

#### promise模型

---

### 服务

接收实时文本数据并进行翻译。片段都带有序号。

- 问题：
	- 代码运行在并发的Web服务器上，要求代码线程安全。

