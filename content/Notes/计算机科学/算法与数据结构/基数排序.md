---
created: 2026-04-30
tags:
  - cs/排序
  - 来源/todo
  - cs/算法
type: course-note
---
基于比较的排序：只需要定义好两个对象之间怎么比较即可。对象的数据特征并不关心，通用。
和比较无关的排序，对对象的数据特征有要求，并不通用。计数排序，样本是整数，且范围比较窄


---

准备桶。按照 个位 十位放。再倒桶![[Pasted image 20260430124352.png]]

同样在二号桶里的 先进来的先出去![[Pasted image 20260430124509.png]]

个位先有序 十位数字若一样 根据个位数字已先排序 相对次序保留；个位一样，十位排后，

## 技巧1 前缀分区


前缀次数累加和。2填倒7位置，最后一个小于等于2的位置。![[Pasted image 20260430125224.png]]
从右往左


## 技巧2 提取数每一位

offset=1
offset\*10
(num/offset)%10


## 代码

根据最大值知道十进制多少位。即几轮。

```java

```



关于负数，转化，减去最小值。
![[Pasted image 20260501125032.png]]


## 代码

```java
int BASE=10;
int MAXN=50001;
int[] help=new int[MAXN];
int[] cnts=new int[BASE];

int[] sortArray(int[] arr){
	if(arr.length>1){
		int n=arr.length;
		int min=arr[0];
		for(int i=1;i<n;i++){
			min=Math.min(min,arr[i]);
		}
		int max=0;
		for(int i=0;i<n;i++){
			//数组中的每个数字，减去数组中的最小值，就把arr转成了非负数组
			arr[i]-=min;
			//记录数组中的最大值
			max=Math.max(max,arr[i]);
		
		}
		//根据最大值在base进制下的位数，决定基数排序做多少轮
		radixSort(arr,n,bits(max));
		//数组中所有数都减去了最小值，最后还原
		for(int i=0;i<n;i++){
			arr[i]+=min;
		}
	}
	return arr;
}

	//返回number在BASE进制下有几位
	public static  int bits(int number){
		int ans=0;
		while(number>0){
			ans++;
			number /=BASE;
		}
		return ans;
	}

	//基数排序核心代码
	//arr内要保证没有负数，n是arr的长度，bits是arr中最大值在BASE进制下有几位
	public static void radixSort(int[] arr,int n,int bits){
		for(int offset=1;bit>0;offset*=BASE,bits--){
			Arrays,fill(cnts,0);
			for(int i=0;i<n;i++){
				//数字提取某一位
				cnts[(arr[i]/offset)%BASE]++;
			}
			//处理成前缀次数累加的形式
			for(int i=1;i<BASE;i++){
				cnts[i]=cnts[i]+cnts[i-1];
			}
			for(int i=n-1;i>=0;i--){
				help[--cnts[(arr[i]/offset)%BASE]]=arr[i];
			}
			for(int i=0;i<n;i++){
				arr[i]=help[i];
			}
		}
	
	}

```