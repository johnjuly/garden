---
created: 2026-04-17
tags:
  - cs/cpp
---
小型的关联数据库  查找快



红黑树。


multi:允许重复key
## multiset


随机数放元素，100万 ，重复机率大。

放入元素
`c.insert(string(buf))` 非头或尾，安插在该在的位置。放的时候做了排序。

## multimap

每个元素两个部分 ,key & value.

`multimap<long,string> c`

组合 key&value
`c.insert(pair<long,string>(i,buf))`

取pair
pair.first  
`(*pItem).second`


---


哈希表

## unordered_multiset

每一个篮子 链不能太长 循序查找  ，篮子数量大于元素个数。
如果元素数量大于篮子数量，篮子扩充两倍。


`c.size()`
`c.buket_count()`
`c.load_factor()`
`c.bucket_size(i)`


## unordered_multimap




key不可重复
## set

## map


插入 i buf 变成pair
`c[i]=string(buf)`
