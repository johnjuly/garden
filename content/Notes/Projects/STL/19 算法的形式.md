---
created: 2026-06-13
tags:
  - cs/stl
  - cs/算法
  - cs/cpp
type: course-note
---
第三讲。

标准库里的算法，是什么？
function template

- 容器，迭代器，仿函数，适配器，分配器都是class template.

```cpp
template<typename Iterator>
Algorithm(Iterator itr1,iterator itr2)
{

}

template<typename Iterator,typename Cmp>
Algorithm(Iterator itr1, Iterator itr2,Cmp cmp)
{
	...//cmp 准则
}

```

![[Pasted image 20260613201415.png]]
- 算法 处理容器里的data,看不见容器，只看得见迭代器。算法所需要的一切信息必须从迭代器取得。迭代器由容器提供。


## 迭代器的分类

使用对象。

random acess 连续空间，可以跳跃
```cpp
struct input_iterator_tag{};
struct output_iterator_tag{};
struct forward_iterator_tag: public input_iterator_tag{};
struct bidirectional_iterator_tag:public forward_iterator_tag{};
struct random_access_iterator_tag:public bidirectional_iterator_tag{};
```


![[Pasted image 20260613202218.png]]


type+()：临时对象 无名称

往上五个重载函数。![[Pasted image 20260613202732.png]]

istream_iterator和ostream_iterator

## 使用 typeid

> `#include <typeinfo>`

本身提供
![[Pasted image 20260613203154.png]]

输出取决于library的实现，名称前后的数字。



## iterator_catrgory对算法的影响


### 例子1 distance

- distance算法：两个指针的距离。两个参数（first,last）,调用1和2两个函数；![[Pasted image 20260704223345.png]]

	- 2 版本：相减，连续空间可以相减(`random_access`);  ![[Pasted image 20260704223440.png]]
	- 1版本： 头与尾的距离无法减，从头一步一步走到尾。(`while`)![[Pasted image 20260704223454.png]]

- 该函数的返回类型，两根指针的距离使用什么类型？使用traits`difference_type`
- 结构：算法本身是一个主函数，根据迭代器分类调用不同次函数
- 可是有四种类别？这里只写了两个类别。继承。is a . 假设是forward_iterator is a input_iterator.![[Pasted image 20260704225145.png]]
- 


### 例子2 advance

![[Pasted image 20260704224609.png]]
- advance,三个版本
	- random access可以跳，直接+
	- 双向的
	- 只能往前
- 小函数iterator_category()取出iterator的category并以此创建一个临时对象。![[Pasted image 20260704224839.png]]


### 例子3 copy



- 来源端 目的端  。 三个指针（来源：起点终点；目的：起点）。
```cpp
template<class InputIterator,class OutputIterator>
OutputIterator copy(InputIterator first,InputIterator last,OutputIterator result){
	while(first!=last){
		*result=*first;
		++result,++first;
	}
	return result;
}
```
![[Pasted image 20260704231624.png]]
- copy分成三个版本，其中一个又分为三种情况...
- 不断检查看收到的迭代器是否属于某种类型，选择更快速的动作。
- 精细，一层一层地判断
- type traits（第四讲）把type放进去，问 op- 拷贝赋值重不重要


### 例子4 destroy()

![[Pasted image 20260704232041.png]]


- 摧毁一个对象，如果没有重要(trivial)的析构函数，不用做什么事情；重要的话 调用析构函数。