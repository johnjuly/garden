---
created: 2026-06-02
tags:
type: course-note
field: cs
topic: 数据结构
---
## 题目1：setall功能的哈希表

常数时间内。O(1)

- 加时间戳 cnt。比较 setalltime和加入修改的时间戳
- 两个小变量 `int setallvalue`  `int setalltime`
![[Pasted image 20260602182715.png]]
get(5) 这里的0和2相比，说明 是9

`map<int,int[]> hashmap`  5,17,0

## 题目2 ：实现LRU结构


空间有限。只能存入规定条记录。淘汰规则，最不经常访问的。

- 双向链表（维持时序）+哈希（保存内存地址）
- 双向链表 头：最早，尾：最晚。
- map记录每个节点的 内存地址。![[Pasted image 20260603103002.png]]
- get(a),  map中直接找到，然后调整时序，节点挂在尾巴后。头节点尾节点移动。
- put(c,25),通过map找， 移出c节点 ，挂在尾
- put(d,13),淘汰掉头节点 
- 所有行为O(1),没有遍历。

put方法：分情况
- 有无该条记录，
	- 有 ：直接从map中get,然后更新链表
	- 无： 
		- 如果map大小达到容量，先从链表和map中移除LRU节点
		- 若没到 直接加到链表和哈希表


## 题目3：实现O(1)的数据结构


增与删。getrandom().

两个结构，map与动态数组。当删除的时候，最后一个元素补到它的位置，实现数组位置连续，从而可以getrandom.









